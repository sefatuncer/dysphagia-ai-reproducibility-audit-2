# Empirical evidence — declared dependency set is unsatisfiable (repo C-repo-001, VFSS_analysis)
# Date: 2026-07-13 | Container: python:3.10-slim | Tool: pip 23.0.1 resolver
# Command:
#   docker run --rm python:3.10-slim pip install "scikit-image==0.25.0" "numpy==1.23.5"
#
# Context: nnunet==1.7.1 (nnU-Net v1, used by run.py) requires numpy<1.24 at runtime
# (uses removed aliases np.bool/np.int). The declared environment also pins
# scikit-image==0.25.0, which requires numpy>=1.24. The two cannot coexist.
#
# --- pip output (tail) ---
ERROR: Cannot install numpy==1.23.5 and scikit-image==0.25.0 because these package versions have conflicting dependencies.

The conflict is caused by:
    The user requested numpy==1.23.5
    scikit-image 0.25.0 depends on numpy>=1.24

ERROR: ResolutionImpossible: for help visit https://pip.pypa.io/en/latest/topics/dependency-resolution/#dealing-with-dependency-conflicts
# --- end ---
#
# Interpretation: "as-declared" reproduction is impossible without dependency surgery.
#
# ============================================================================
# SECOND, STRONGER evidence — from an actual best-effort build (2026-07-13):
# When resolving the declared deps against numpy==1.23.5, pip reported that
# MULTIPLE declared packages require numpy>=1.24, not just scikit-image:
#
#   ERROR: Cannot install medpy==0.5.2, numpy==1.23.5, pandas==2.0.3,
#   scikit-image==0.19.3, scikit-learn==1.3.2 and scipy==1.10.1 because these
#   package versions have conflicting dependencies.
#   The conflict is caused by:
#       The user requested numpy==1.23.5
#       scikit-image 0.25.0 depends on numpy>=1.24
#       medpy 0.5.2 depends on numpy>=1.24          <-- second offender
#   ERROR: ResolutionImpossible
#
# => The declared environment requires numpy>=1.24 via at least TWO packages
#    (scikit-image 0.25.0 AND MedPy 0.5.2) while nnU-Net v1.7.1 requires numpy<1.24.
#    A working run required downgrading numpy 2.1.3->1.23.5, scikit-image 0.25->0.19.3,
#    AND MedPy 0.5.2->0.4.0 (three forced deviations). This is the quantified friction.
# ============================================================================
